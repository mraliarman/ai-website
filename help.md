
## نقش تو (Role)

تو یک **مهندس فرانت‌اند ارشد + معمار اطلاعات + متخصص سئو/امنیت/کارایی** هستی که مسئولیت کامل ساخت، توسعه و push مستقیم یک وب‌سایت آموزشی استاتیک (بدون بک‌اند) رو به عهده داری. تو به ریپازیتوری گیت‌هاب من دسترسی مستقیم داری (از طریق ابزار اتصال به گیت‌ها). باید:

1. تغییرات رو مستقیماً روی یک برنچ جدید (`feature/build-website`) کامیت و پوش کنی.
2. بعد از هر مرحله بزرگ، یک Pull Request به `main` باز کنی با توضیح کامل تغییرات.
3. اگر جایی به تصمیم من نیاز داشت (مثلاً انتخاب دامنه، اسم نهایی سایت)، سوال رو مشخص و کوتاه بپرس و کار رو متوقف نکن — فرض منطقی بذار و در PR description قید کن که قابل تغییره.

---

## ۱. معرفی پروژه

- **نام پروژه:** یک وب‌سایت آموزش کار با هوش مصنوعی و مدل‌های زبانی بزرگ (LLM)، به زبان فارسی.
- **ریپازیتوری مقصد (که باید توش کد بنویسی):** ریپازیتوری‌ای که الان بهش متصلی (من به تو دسترسی دادم).
- **منبع محتوا:** `https://github.com/3lf/llm-for-humans` — تمام محتوای فایل `README.md` این ریپو (و هر فایل دیگه‌ای که توش هست، مثل تصاویر پوشه `images`) باید به‌صورت کامل خونده، دسته‌بندی و به‌صورت صفحات جداگانه‌ی HTML/محتوایی داخل وب‌سایت جدید بازتولید بشه. **هیچ بخشی از محتوای اون ریپو نباید جا بیفته.**
- **بدون بک‌اند:** پروژه باید ۱۰۰٪ استاتیک باشه (HTML/CSS/JS ساده، قابل هاست روی GitHub Pages / Netlify / Vercel Static / Cloudflare Pages). هیچ سرور، دیتابیس، یا API اختصاصی نباید داشته باشه. جستجو هم باید کاملاً سمت کلاینت (Client-side) پیاده بشه.
- **تکنولوژی:**
  - **Tailwind CSS v4** (ترجیحاً از طریق CLI/Build با فایل و خروجی CSS بهینه‌شده و purge‌شده؛ نه از CDN، چون CDN برای پروداکشن مناسب نیست و روی سرعت/سئو اثر منفی داره).
  - **Alpine.js** برای تعامل‌های سبک (منوی موبایل، تب‌ها، آکاردئون‌ها، مودال جستجو، فیلتر دسته‌بندی).
  - **Vite** به‌عنوان باندلر/دو سرور توسعه (ساده‌ترین راه برای build کردن Tailwind + چند صفحه‌ی HTML استاتیک + خروجی بهینه).
  - داده‌های محتوا (مقالات، پرامپت‌ها، دسته‌بندی‌ها) به‌صورت فایل‌های **JSON یا Markdown** نگه داشته بشن تا نگهداری و افزودن محتوای جدید در آینده ساده باشه.
  - برای جستجوی حرفه‌ای سمت کلاینت از **Fuse.js** یا **lunr.js** استفاده کن (کتابخونه‌ی سبک، بدون نیاز به سرور، ایندکس‌سازی build-time).

---

## ۲. سیستم طراحی (Design System) — باید دقیقاً همین باشه

سبک بصری سایت باید **دقیقاً** مطابق این سند طراحی («Hyperstudio») باشه؛ یه پس‌زمینه‌ی تقریباً مشکی با تایپوگرافی درشت، خطوط باریک به‌جای سایه، و بدون رنگ اضافه. همه‌چیز باید با متغیرهای CSS زیر پیاده بشه، نه رنگ‌های دستی و پراکنده.

### پالت رنگ
| نام | مقدار | نقش |
|---|---|---|
| Obsidian | `#101010` | پس‌زمینه‌ی اصلی صفحه |
| Carbon | `#080808` | لایه‌ی عمیق‌تر (هیرو، کارت‌ها) |
| Chalk | `#f3f3f3` | متن اصلی |
| Smoke | `#9c9c9c` | متن ثانویه/توضیحات |
| Ash | `#c1c1c1` | بوردر میان‌رده، متن سوم |
| Graphite | `#212121` | بوردر اصلی ۱px (خط تفکیک‌کننده‌ی بخش‌ها — پرکاربردترین المان بصری) |
| Iron | `#474747` | بوردر ثانویه |
| Signal White | `#ffffff` | دکمه‌ی اصلی (fill سفید + متن مشکی) — تنها رنگ پرکنتراست اکشن |
| Compass Gold | `#6f6759` | فقط برای stroke آیکون‌ها (هرگز متن یا پس‌زمینه) |
| Card Slate | `#3b3d45` | بوردر تزئینی کارت‌های سطح بالا |
| Pulse Green | `#98ff38` | فقط برای نقطه‌ی وضعیت زنده/فعال (مثلاً «بروزرسانی جدید») |

### تایپوگرافی
- فونت اصلی: → از **Inter** یا **Vazirmatn** (برای پشتیبانی درست فارسی/RTL از Vazirmatn به‌عنوان جایگزین اصلی استفاده کن) با وزن ۴۰۰ برای همه‌چیز، هیچ‌جا Bold استفاده نکن مگر واقعاً لازم باشه.
- فونت ثانویه (برای لیبل‌ها، متادیتا، بج‌ها): معادل «Input» → **IBM Plex Mono** یا **Vazirmatn Mono** اگر موجود بود، وگرنه یک مونو ساده.
- مقیاس تایپوگرافی: caption 13px / body 16px / heading-xs 18px / subheading 21px / heading-sm 23px / heading 34px / heading-lg 44px (tracking -0.31px) / display 63px (line-height 1.05, tracking -0.69px).
- **نکته‌ی مهم RTL:** letter-spacing منفی روی فارسی معمولاً بد به‌نظر می‌رسه و می‌تونه اتصال حروف رو خراب کنه. مقدار tracking رو برای حالت فارسی (`dir="rtl"`) کمتر یا صفر کن، ولی اندازه‌ها و سلسله‌مراتب وزنی (۴۰۰ همه‌جا، بدون بولد) رو دقیقاً حفظ کن.

### فاصله‌گذاری و شکل‌ها
- واحد پایه: 4px. مقیاس: 4/8/12/16/20/24/40px.
- Border-radius: تگ‌ها 4px، کارت‌ها 8px، آیکون‌ها 99px (دایره)، دکمه‌ها 9999px (pill).
- بدون سایه (drop-shadow) در هیچ‌جای سایت. الویشن فقط با بوردر ۱px گرافیت یا کنتراست رنگ ایجاد میشه.
- عرض محتوا حداکثر 1200px، وسط‌چین. فاصله‌ی بین بخش‌ها 120–210px.

### کامپوننت‌های الزامی (دقیقاً طبق مشخصات بساز)
1. **دکمه‌ی Pill سفید (اکشن اصلی):** پرکردن سفید، متن Obsidian، radius کامل، پدینگ 12px 24px، فونت 14px حروف بزرگ (uppercase).
2. **دکمه‌ی Ghost خطی (اکشن ثانویه):** بدون پس‌زمینه، بوردر سفید ۱px، radius 8px.
3. **بج وضعیت (Status Badge):** پس‌زمینه‌ی تیره #1a1a1a، بوردر گرافیت، نقطه‌ی سبز Pulse برای «جدید»/«بروزشده».
4. **کارت دسته‌بندی/مقاله (Grid Cell):** بدون پرکردن، فقط بوردر گرافیت، آیکون خطی طلایی، عنوان uppercase.
5. **خط تفکیک‌کننده‌ی بخش (Section Divider):** یک خط افقی ۱px گرافیت — عنصر ساختاری اصلی صفحه.
6. **نوار ناوبری بالا:** شفاف روی پس‌زمینه، لوگو سمت راست (چون RTL هست، آینه‌ی نسخه‌ی انگلیسی رو بزن)، لینک‌های ناوبری وسط، دکمه‌ی pill سمت چپ (یا جستجو).
7. **بلوک هدلاین:** تایپوگرافی درشت 63px برای هیرو و عنوان صفحات اصلی.
8. **فوتر:** بوردر بالای گرافیت، بدون پس‌زمینه، متن تماس/لینک‌ها.

از این قوانین طراحی هرگز خارج نشو: بدون سایه، بدون رنگ اضافه غیر از موارد بالا، بدون بولد در هدینگ‌ها، رادیوس محدود به مقادیر تعریف‌شده.

پیاده‌سازی این توکن‌ها رو به‌صورت متغیرهای Tailwind v4 (`@theme`) در فایل CSS اصلی انجام بده (رنگ‌ها، فونت‌ها، اسپیسینگ، رادیوس، همگی به‌عنوان custom token، نه کلاس‌های hardcoded پراکنده در HTML).

---

## ۳. معماری اطلاعات و دسته‌بندی محتوا

محتوای `README.md` ریپوی `3lf/llm-for-humans` رو کامل بخون و طبق فهرست مطالب خودش، به دسته‌های زیر تقسیم کن (هر دسته = یک بخش/صفحه‌ی مجزا در سایت با URL خودش، نه یک صفحه‌ی طولانی تک‌صفحه‌ای):

1. **مبانی LLM** — LLM چیست، توکن، امبدینگ، پیش‌بینی توکن بعدی.
2. **پرامپت‌نویسی پایه** — تعریف پرامپت، اسکلت یک پرامپت خوب، مثال‌های ساده.
3. **تکنیک‌های پیشرفته‌ی پرامپت‌نویسی** — جداکننده‌ها، خروجی ساختاریافته، لحن، شرط‌گذاری، Few-Shot، Chain-of-Thought، تکرار و بهبود.
4. **پارامترهای مدل** — Temperature، Top-p، Frequency/Presence Penalty، Top-k.
5. **سیستم پرامپت (System Prompt)**.
6. **تکنیک‌های حرفه‌ای پرامپت‌نویسی** — Role Prompting، Few-Shot، CoT، ToT، ReAct، Self-Consistency، Self-Reflection، CoVe، Multi-Agent Debate.
7. **مشکلات معروف LLM‌ها و راهکار** — عدم استناد، Bias، Hallucination، خطای ریاضی، Prompt Hacking، محدودیت Context Window، دانش قدیمی، عدم درک احساسی.
8. **افزایش دقت و امنیت LLM** — تمام هشت راهکار ریپو (Smart Prompting، Moderation، Fact-Checking، Tool Integration، Fine-tuning دوره‌ای، Self-Evaluation، Context Management، Defense in Depth).
9. **ارزیابی پرامپت و خروجی** — Human Eval، LLM-as-a-Judge، Auto Metrics، Rubric، A/B Test.
10. **معماری و شخصی‌سازی** — Fine-tuning vs RAG، LoRA/QLoRA، دیتای فاین‌تیون، مراحل عملی.
11. **مهندسی کانتکست (Context Engineering)**.
12. **RAG در عمل** — معماری، Chunking، Retrieval، ارزیابی RAG.
13. **ابزارها و خروجی ساختاریافته (Function Calling / MCP)**.
14. **ساخت پرامپت با کمک ابزار** — پرامپت‌سازها، سایت‌های پرامپت‌ساز.
15. **انتخاب مدل و Provider**.
16. **دسترسی رایگان به API و اجرای محلی** — Ollama، LM Studio.
17. **⭐ پرامپت‌های حرفه‌ای برنامه‌نویسی (بخش اختصاصی، جدا از محتوای ریپو)** — توضیح کامل در بخش ۴ همین سند.

> برای هر دسته یک اسلاگ (slug) انگلیسی و تمیز بساز (مثل `/fundamentals`, `/prompting-basics`, `/advanced-techniques`, `/parameters`, `/system-prompt`, `/pro-techniques`, `/known-issues`, `/accuracy-security`, `/evaluation`, `/finetuning-vs-rag`, `/context-engineering`, `/rag-in-practice`, `/tool-use-mcp`, `/prompt-builders`, `/model-selection`, `/free-access`, `/developer-prompts`).

هر مقاله باید فراداده (metadata) استاندارد داشته باشه: `id`, `title`, `slug`, `category`, `tags[]`, `summary`, `content` (Markdown رندرشده به HTML)، `readingTime`، `order`. این‌ها رو در فایل‌های `src/content/articles/*.md` (با frontmatter) نگه‌دار و در زمان build به یک `search-index.json` تبدیلشون کن.

---

## ۴. مجموعه‌ی پرامپت‌های حرفه‌ای برای برنامه‌نویس/توسعه‌دهنده (بخش ویژه)

این بخش، محتوای اختصاصی خودِ این پروژه است (نه از ریپوی مرجع) و باید توسط خودت (ChatGPT) نوشته و کامل بشه. هدف: مجموعه‌ای کامل از پرامپت‌های آماده که یک برنامه‌نویس/توسعه‌دهنده بتونه با استفاده از یک دستیار هوش مصنوعی (مثل ChatGPT/Claude/Copilot) یک محصول نرم‌افزاری رو **از صفر تا صد راه‌اندازی کنه** و در ادامه ایرادات امنیتی، سرعت و سئو رو هم بهبود بده.

این پرامپت‌ها رو در قالب **دسته‌بندی‌های زیر** بساز (هر دسته حداقل ۵ تا ۱۰ پرامپت آماده‌ی کپی‌پیست، هرکدوم با: عنوان، توضیح یک‌خطی، متن کامل پرامپت در یک بلوک کد قابل کپی، و برچسب سطح دشواری):

1. **کشف نیاز و برنامه‌ریزی محصول** (Product Discovery) — نوشتن PRD، تعریف MVP، شکستن پروژه به Epic/Task.
2. **معماری نرم‌افزار** — انتخاب استک، طراحی دیتابیس، طراحی API (REST/GraphQL)، تصمیم‌های معماری (ADR).
3. **راه‌اندازی اولیه‌ی پروژه (Scaffolding)** — ساخت اسکلت پروژه فرانت/بک/دیتابیس، تنظیم لینتر/فرمتر/CI.
4. **توسعه‌ی فرانت‌اند** — ساخت کامپوننت، مدیریت state، فرم‌ها، دسترسی‌پذیری (a11y).
5. **توسعه‌ی بک‌اند** — نوشتن endpoint، اعتبارسنجی ورودی، مدیریت خطا، احراز هویت/مجوز.
6. **دیتابیس و مدل‌سازی داده** — طراحی اسکیما، migration، ایندکس‌گذاری، بهینه‌سازی کوئری.
7. **تست‌نویسی** — Unit test، Integration test، E2E، تولید داده‌ی تست.
8. **بازبینی کد و رفع باگ (Code Review & Debug)** — پرامپت‌های بررسی PR، پیدا کردن ریشه‌ی باگ، ریفکتور.
9. **🔐 امنیت (Security Audit)** — پرامپت‌هایی برای پیدا کردن: SQL Injection، XSS، CSRF، مدیریت اشتباه سکرت‌ها/کلیدها، وابستگی‌های آسیب‌پذیر، تنظیمات نادرست CORS/Headers، بررسی OWASP Top 10، پرامپت تولید چک‌لیست امنیتی قبل از انتشار.
10. **⚡ کارایی و سرعت (Performance)** — پرامپت‌هایی برای: پیدا کردن N+1 Query، بهینه‌سازی باندل فرانت (Code Splitting، Lazy Load)، بهینه‌سازی تصاویر، کش‌گذاری (HTTP Cache/CDN)، پروفایل کردن رندر، بهبود Core Web Vitals (LCP/CLS/INP).
11. **🔍 سئو (SEO)** — پرامپت‌هایی برای: تولید متادیتای صحیح (title/description/canonical)، Structured Data (JSON-LD)، Sitemap/robots.txt، بهینه‌سازی محتوا برای سرچ، بررسی رندر سمت سرور/Prerendering برای موتورهای جستجو، بررسی لینک‌های شکسته و ساختار هدینگ‌ها.
12. **DevOps و استقرار (Deployment)** — نوشتن Dockerfile، تنظیم CI/CD، متغیرهای محیطی، مانیتورینگ و لاگ.
13. **مستندسازی** — نوشتن README حرفه‌ای، مستندات API، راهنمای مشارکت (CONTRIBUTING).
14. **بازبینی نهایی پیش از انتشار (Pre-Launch Checklist)** — یک پرامپت جامع که تمام موارد بالا (امنیت + سرعت + سئو + تست) رو یک‌جا از دستیار هوش مصنوعی می‌خواد چک کنه.

هر پرامپت باید:
- **قابل استفاده‌ی مستقیم** باشه (متغیرهای قابل تغییر داخلش با `[کروشه]` مشخص بشه، مثلاً `[نام پروژه]`, `[زبان برنامه‌نویسی]`, `[فریم‌ورک]`).
- ساختار حرفه‌ای داشته باشه: نقش (Role) + هدف (Goal) + قید و محدودیت (Constraints) + فرمت خروجی موردنیاز.
- فارسی روان بنویس، ولی اسم‌های فنی (مثل `SQL Injection`, `Core Web Vitals`) رو انگلیسی نگه‌دار.

این دسته (`/developer-prompts`) باید دارای فیلتر بر اساس زیر‌دسته (۱۴ مورد بالا)، سطح (مبتدی/متوسط/حرفه‌ای)، و دکمه‌ی «کپی پرامپت» (با Alpine.js + Clipboard API) باشه.

---

## ۵. ویژگی‌های فنی الزامی

### الف) جستجوی حرفه‌ای
- در زمان build، از تمام مقالات و پرامپت‌ها یک ایندکس JSON بساز (`public/search-index.json`).
- در کلاینت با **Fuse.js** جستجوی fuzzy روی عنوان، خلاصه، تگ‌ها و متن پیاده کن.
- UI: یک دکمه‌ی جستجو در navbar که با کلیک (یا کلید میانبر `/`) یک مودال تمام‌صفحه با overlay مشکی نیمه‌شفاف باز می‌کنه، اینپوت جستجو، و نتایج زنده (debounce ۱۵۰ میلی‌ثانیه) با هایلایت کلمه‌ی جستجو‌شده و دسته‌بندی نتیجه.
- پشتیبانی کامل از تایپ فارسی (نرمال‌سازی ی/ک عربی به فارسی، حذف نیم‌فاصله در نرمال‌سازی جستجو تا نتایج بهتر بشه).

### ب) دسته‌بندی
- صفحه‌ی اصلی: نمایش همه‌ی دسته‌ها به‌صورت گرید کارت (مطابق کامپوننت Service Card بالا).
- هر دسته یک صفحه‌ی فهرست مقالات داره + امکان فیلتر بر اساس تگ.
- Breadcrumb (مسیر ناوبری) در بالای هر صفحه‌ی مقاله.

### ج) صفحه‌بندی و ناوبری مقاله
- دکمه‌ی «مقاله‌ی بعدی/قبلی» بر اساس ترتیب فهرست مطالب اصلی ریپو.
- فهرست محتوا (Table of Contents) خودکار در سمت چپ صفحه برای مقالات طولانی (اسکرول‌اسپای با Alpine.js).

### د) ریسپانسیو و دسترسی‌پذیری
- Mobile-first، منوی همبرگری روی موبایل.
- کنتراست رنگ مطابق استاندارد WCAG AA حتی با وجود تم تیره (روی پس‌زمینه‌ی #101010 رنگ‌های Chalk/Smoke این استاندارد رو رعایت می‌کنن — چک کن).
- تمام تصاویر `alt` داشته باشن، تمام دکمه‌ها `aria-label` مناسب.

---

## ۶. رعایت امنیت، سرعت و سئو در خودِ این پروژه (نه فقط آموزششون)

چون موضوع سایت آموزش بهبود امنیت/سرعت/سئو است، خودِ سایت باید نمونه‌ی خوبی از رعایت این اصول باشه:

**سئو:**
- هر صفحه: `<title>` و `<meta description>` منحصربه‌فرد، تگ `canonical`.
- Open Graph + Twitter Card برای هر مقاله.
- `sitemap.xml` و `robots.txt` خودکار در زمان build تولید بشه.
- Structured Data از نوع `Article`/`BreadcrumbList` (JSON-LD) برای هر مقاله.
- ساختار هدینگ صحیح (فقط یک `h1` در هر صفحه، سلسله‌مراتب منطقی h2/h3).
- چون سایت کاملاً استاتیک با Vite ساخته می‌شه، HTML نهایی از قبل رندرشده و برای موتورهای جستجو قابل خزیدنه (نیازی به SSR اضافه نیست).

**سرعت:**
- Tailwind با purge کامل (فقط کلاس‌های استفاده‌شده در خروجی باشه).
- فونت‌ها با `font-display: swap` و preload فونت اصلی.
- تصاویر با فرمت `webp`/`avif`، `loading="lazy"` برای تصاویر پایین صفحه.
- کد جاوااسکریپت حداقلی (Alpine.js + Fuse.js فقط، بدون فریم‌ورک سنگین).
- خروجی build شده و minify شده، بدون منبع خارجی غیرضروری (فونت‌ها و آیکون‌ها لوکال باشن، نه از CDN خارجی که کندی و ریسک privacy میاره — مگر jsDelivr/cdnjs برای خود Alpine.js/Fuse.js که قابل قبوله).

**امنیت:**
- چون بک‌اند نداریم، سطح حمله محدوده؛ ولی رعایت کن:
  - هدرهای امنیتی پیشنهادی (`Content-Security-Policy`, `X-Content-Type-Options`, `Referrer-Policy`) رو در یک فایل `_headers` (برای Netlify/Cloudflare Pages) یا نمونه‌ی تنظیمات Nginx مستند کن.
  - هیچ داده‌ی حساس/کلید API در کد فرانت قرار نگیره.
  - لینک‌های خارجی با `rel="noopener noreferrer"`.
  - فرم‌ها (اگر جایی مثل «تماس با ما» بود) به یک سرویس third-party امن (مثل Formspree) وصل بشن، نه به بک‌اند اختصاصی.

---

## ۷. ساختار پیشنهادی پوشه‌ها

```
.
├── src/
│   ├── content/
│   │   ├── articles/           # فایل‌های .md با frontmatter برای هر مقاله
│   │   └── developer-prompts/  # فایل‌های .md یا .json برای هر پرامپت برنامه‌نویسی
│   ├── data/
│   │   └── categories.json
│   ├── layouts/                # قالب‌های مشترک (Header, Footer, ArticleLayout)
│   ├── styles/
│   │   └── main.css            # شامل @theme توکن‌های طراحی
│   └── scripts/
│       ├── build-search-index.js
│       └── main.js             # Alpine.js init + رفتارهای سراسری
├── public/
│   ├── images/
│   ├── search-index.json       # تولیدشده در زمان build
│   ├── sitemap.xml             # تولیدشده در زمان build
│   └── robots.txt
├── index.html
├── [category]/index.html ...   # صفحات دسته‌بندی
├── article/[slug]/index.html   # صفحات مقاله (تولید استاتیک)
├── tailwind.config.js
├── vite.config.js
├── package.json
└── README.md
```

---

## ۸. مراحل اجرا (گام‌به‌گام — دقیقاً به همین ترتیب پیش برو)

**مرحله ۰ — آماده‌سازی**
- یک برنچ جدید `feature/build-website` بساز.
- `README.md` فعلی ریپوی مقصد رو بخون تا محدودیت‌ها/قراردادهای قبلی رو بفهمی (اگر ریپو خالیه، رد شو).

**مرحله ۱ — راه‌اندازی پروژه**
- پروژه‌ی Vite (Vanilla یا Multi-Page) بساز، Tailwind CSS و Alpine.js رو نصب و پیکربندی کن.
- توکن‌های طراحی بخش ۲ رو در `tailwind.config.js` / `@theme` پیاده کن. فونت Vazirmatn رو نصب/preload کن، `dir="rtl"` و `lang="fa"` رو در `<html>` تنظیم کن.
- کامپوننت‌های پایه (Navbar، Footer، Button، Card، Section Divider، Search Modal) رو به‌صورت partial/template بساز.
- کامیت کن: `chore: scaffold project with vite + tailwind + alpine, apply design tokens`.

**مرحله ۲ — استخراج و ساختاردهی محتوا**
- محتوای کامل `README.md` مخزن `3lf/llm-for-humans` رو بخون (از طریق فچ مستقیم فایل خام از گیت‌هاب).
- طبق دسته‌بندی بخش ۳، محتوا رو به فایل‌های Markdown جدا با frontmatter تقسیم کن (حفظ امانت در محتوا؛ فقط ساختار و قالب‌بندی رو استاندارد کن، مثال‌های کد و جدول‌ها رو دقیق منتقل کن).
- تصاویر پوشه‌ی `images` ریپوی مبدا رو (در صورت وجود لایسنس/اجازه‌ی استفاده — پوستر معرفی و غیره) دانلود و در `public/images` قرار بده؛ در غیر این صورت جایگزین بصری متناسب با سیستم طراحی (آیکون خطی/دات‌مپ) بساز.
- کامیت کن: `content: import and structure all articles from source repository`.

**مرحله ۳ — تولید بخش «پرامپت‌های حرفه‌ای برنامه‌نویس»**
- طبق بخش ۴ همین سند، تمام ۱۴ زیر‌دسته رو با حداقل ۵ پرامپت هرکدوم (جمعاً حداقل ۷۰ پرامپت) بنویس و در `src/content/developer-prompts/` ذخیره کن.
- کامیت کن: `content: add professional developer prompts collection`.

**مرحله ۴ — پیاده‌سازی صفحات**
- صفحه‌ی اصلی (هیرو + گرید دسته‌بندی‌ها + معرفی کوتاه پروژه + CTA به بخش پرامپت‌های برنامه‌نویس).
- صفحه‌ی هر دسته (فهرست مقالات + فیلتر تگ).
- صفحه‌ی هر مقاله (محتوا + TOC + ناوبری بعدی/قبلی + breadcrumb).
- صفحه‌ی اختصاصی «پرامپت‌های برنامه‌نویس» (فیلتر زیر‌دسته/سطح + دکمه‌ی کپی).
- صفحه‌ی «درباره/کمک به پروژه» (بر اساس بخش پایانی ریپوی مبدا).
- کامیت کن: `feat: implement all pages and templates`.

**مرحله ۵ — جستجو**
- اسکریپت `build-search-index.js` رو بساز که در زمان build همه‌ی محتوا رو جمع کرده و `search-index.json` رو تولید کنه.
- مودال جستجو با Fuse.js رو پیاده‌سازی کن.
- کامیت کن: `feat: add client-side full-text search with fuse.js`.

**مرحله ۶ — سئو و سرعت**
- متادیتا، sitemap، robots.txt، JSON-LD رو طبق بخش ۶ اضافه کن.
- تصاویر رو بهینه کن (webp/avif + lazy loading)، فونت‌ها رو preload کن، خروجی build رو با Lighthouse/PageSpeed (به‌صورت تئوری در توضیحات PR) اعتبارسنجی کن و اهداف Core Web Vitals رو مستند کن.
- کامیت کن: `perf: optimize images, fonts, and add SEO metadata`.

**مرحله ۷ — تست و QA**
- تست کن که در موبایل/دسکتاپ درست نمایش داده می‌شه، RTL درست کار می‌کنه، جستجو نتیجه‌ی درست می‌ده، لینک‌های داخلی خراب نیستن.
- یک چک‌لیست QA در PR description بنویس.

**مرحله ۸ — مستندسازی و انتشار**
- `README.md` نهایی پروژه رو بنویس: معرفی، نحوه‌ی اجرا (`npm install && npm run dev`)، نحوه‌ی build (`npm run build`)، نحوه‌ی افزودن مقاله‌ی جدید، نحوه‌ی افزودن پرامپت جدید، نحوه‌ی دیپلوی (GitHub Pages/Netlify/Vercel).
- یک فایل `CONTRIBUTING.md` کوتاه اضافه کن.
- Pull Request نهایی رو با خلاصه‌ی کامل تغییرات، اسکرین‌شات (در صورت امکان)، و چک‌لیست تکمیل‌شده باز کن.

---

## ۹. معیار پذیرش نهایی (Definition of Done)

- [ ] پروژه بدون بک‌اند، صرفاً استاتیک، با `npm run build` قابل تولید و روی هر هاست استاتیکی قابل انتشار است.
- [ ] تمام محتوای ریپوی مرجع، بدون افتادگی، دسته‌بندی و در سایت موجود است.
- [ ] بخش اختصاصی «پرامپت‌های حرفه‌ای برنامه‌نویس» با حداقل ۱۴ زیر‌دسته و ۷۰+ پرامپت آماده وجود دارد.
- [ ] جستجوی سراسری سمت کلاینت روی عنوان/خلاصه/تگ/متن کار می‌کند و برای فارسی درست نرمال‌سازی شده است.
- [ ] سیستم طراحی (رنگ، تایپوگرافی، فاصله‌گذاری، کامپوننت‌ها) دقیقاً طبق بخش ۲ پیاده شده — بدون سایه، بدون رنگ اضافه، وزن فونت فقط ۴۰۰.
- [ ] سایت کاملاً RTL و فارسی است، ریسپانسیو است، و استانداردهای پایه‌ی دسترسی‌پذیری را رعایت می‌کند.
- [ ] سئو (متادیتا، sitemap، robots.txt، JSON-LD) و بهینه‌سازی سرعت (purge CSS، فونت preload، تصاویر lazy/webp) پیاده‌سازی شده.
- [ ] هدرهای امنیتی پیشنهادی مستند شده و هیچ داده‌ی حساسی در کد نیست.
- [ ] همه‌چیز به‌صورت کامیت‌های منظم و معنادار روی برنچ `feature/build-website` پوش شده و یک Pull Request نهایی باز شده است.

---

## ۱۰. لحن و زبان

- تمام محتوای رابط کاربری (دکمه‌ها، منوها، پیام‌ها) و مقالات باید **فارسی روان و خودمونی** (هم‌راستا با لحن ریپوی مبدا) باشد، نه ترجمه‌ی خشک و رسمی.
- اصطلاحات فنی رایج (Prompt، Token، API، RAG و...) می‌توانند به همان شکل انگلیسی/رایج در متن فارسی استفاده شوند، همان‌طور که در ریپوی مبدا هم همین‌طور است.

---

شروع کن از مرحله‌ی ۰. بعد از هر مرحله، خلاصه‌ی کوتاهی از کاری که کردی و کامیتی که زدی بنویس و به مرحله‌ی بعد برو، بدون اینکه منتظر تایید من بمونی — مگر جایی که واقعاً به یک تصمیم غیرقابل‌حدس از طرف من نیاز داشته باشی.