# راهنمای مشارکت

## توسعه محلی

```bash
npm install
npm run dev
```

## بررسی قبل از PR

```bash
npm run build
```

تغییرات محتوا باید ساختار Markdown و frontmatter را حفظ کنند. برای JavaScript از Alpine.js استفاده کنید و از افزودن framework سنگین خودداری کنید. طراحی باید RTL، بدون shadow و مبتنی بر tokenهای تعریف‌شده در `src/styles/main.css` باقی بماند.

## Pull Request

عنوان PR باید کوتاه و مشخص باشد. در توضیحات PR، تغییرات، تست انجام‌شده و هر تصمیم قابل تغییر را ثبت کنید.
